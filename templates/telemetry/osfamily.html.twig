<div id="os_family" class="pie-size p-1 col border rounded-3 bg-white"></div>

<script>
window.addEventListener("DOMContentLoaded", (event) => {
    const chartDom = document.getElementById("os_family");
    const myChart = echarts.init(chartDom);
    option = {
        title: {
            text: 'Operating Systems',
            left: 'center'
        },
        tooltip: {
            trigger: 'item'
        },
        series: [
            {
            name: 'version users',
            type: 'pie',
            radius: ['40%', '70%'],
            avoidLabelOverlap: false,
            itemStyle: {
                borderRadius: 10,
                borderColor: '#fff',
                borderWidth: 2
            },
            label: {
                show: false,
                position: 'center'
            },
            emphasis: {
                label: {
                show: true,
                fontSize: 40,
                fontWeight: 'bold'
                }
            },
            labelLine: {
                show: false
            },
            data: [
                {% for row in osdata %}
                { value: {{ row.count }}, name: '{{ row.os_family }}' },
                {% endfor %}
            ]
            }
        ]
    };
    option && myChart.setOption(option);

});
</script>
