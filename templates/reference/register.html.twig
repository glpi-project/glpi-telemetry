{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('build/styles/reference.css') }}" />
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('build/js/reference.js') }}" defer></script>
    <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" defer></script>
{% endblock %}

{% block body_content %}
<div class="page bg-white">
    <div class="container-xl">
        <h1 class="py-3">Register your GLPI instance</h1>
        <p class="pb-3 m-0">We maintain a public database of registered GLPI installations. We invite you to register your own GLPI instance.</p>
    </div>
    <div class="form-fieldset container-xl mt-3 mb-3">
        {{ form_start(form, {'attr': {'id': 'reference_form'}}) }}
                <div class="mb-3">
                    {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.url, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.country, {'attr': {'class': 'form-select'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.phone, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.email, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.referent, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.nb_assets, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.nb_helpdesk, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.comment, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="d-flex">"
                    <div class="cf-turnstile" data-sitekey="{{ captchaSiteKey }}" data-response-field-name="captcha_token"></div>
                    <button type="submit" class="btn btn-primary ms-auto">
                        <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"></path><path d="M12 5l0 14"></path><path d="M5 12l14 0"></path></svg>
                        Register
                    </button>
                </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}